mixin specifications(time, size, roles, skills, tools)
  aside.specifications.text-component
    h4 timeline
    p #{time}
    h4 team size
    p #{size}
    h4 role
    p
      for role in roles
        | #{role} 
        br
    h4 skills
    p
      for skill in skills
        | #{skill} 
        br
    h4 tools
    p.last
      for tool in tools
        | #{tool} 
        br